# Backspace Coding Agent Test

This is a test repository for the Backspace Coding Agent. It currently does not contain any specific functionality.

## Installation

To install this project, follow these steps:

1. Clone the repository: `git clone https://github.com/deryakendircikahraman/backspace-coding-agent-test.git`
2. Navigate into the project directory: `cd backspace-coding-agent-test`

## Usage

This is a test repository and does not contain any specific functionality. However, you can run the project with the following command:

`npm start`
